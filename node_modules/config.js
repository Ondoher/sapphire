var mootools = require('mootools').apply(GLOBAL);

var config =
{
	port : 8088,
	server : 'localhost:8088',
	baseUrl : 'http://localhost:8088/',
	basePath : './',
	log : true,
	defaultApp : 'xp',
};

var env = process.env.node_env;

envConfig = {};
if (env)
	envConfig = require('config/config.' + env);

exports.CONFIG = Object.merge(config, envConfig);



